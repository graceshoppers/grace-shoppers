import React from 'react';
import CartItemList from './CartItemList';

const Cart = props => {
  return <CartItemList />;
  // Thinking about also have a checkout section
  // next to the item list
};

export default Cart;
